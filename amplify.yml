version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Frontend
        - nvm use 20
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: Frontend/.next
    files:
      - '**/*'
  cache:
    paths:
      - Frontend/node_modules/**/*
